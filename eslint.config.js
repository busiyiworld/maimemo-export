import ourongxing from "@ourongxing/eslint-config"

export default ourongxing(
  {
    type: "app",
    react: false,
    stylistic: {
    },
    ignores: ["node_modules/**"],
    overrides: {
    },
  },
)
